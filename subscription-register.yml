---
- name: Subscription Register !
  hosts: all
 
  tasks:
 
  - name: Register Subscription 
    redhat_subscription: state=present username={{ USER_NAME }} password={{ PASSWD }} pool='^Employee SKU$'
